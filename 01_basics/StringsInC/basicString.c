#include<stdio.h>
int main(){
    // char str1[]={'m','e','h','u','l','\0'};
    // char str1="mehul";
    // char str1[50];
    // scanf("%s",&str1); For a single word String
    // gets(str1); for multiword String 
    // Put(str1); print the string and have a new line character , while printf does not have it
    // char * str1="harry"; this will give str the address of 'h'
    // String Operations: include strlib.h header file
    // strlen(str1): gives the length of the string 
    // strcpy(target,source): target should have enough capacity toh hold  source
    // strcat(str1,str2): this will concatenate string 2 to string 1 , so string 1 will have the new string
    //strcmp(str1,str2): Gives 0 for same string, gives positive if the first mismatched character of first 
    // string is smaller and negative if it is larges ; 
    // In c characters are powered by ASCII values
     
    return 0;
}